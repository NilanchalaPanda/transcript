# events {
#     worker_connections 1024;
# }

# http {
#     # Include MIME types
#     include /opt/homebrew/etc/nginx/mime.types;
#     default_type application/octet-stream;

#     # Logging
#     access_log /tmp/nginx-access.log;
#     error_log /tmp/nginx-error.log;

#     # Server configuration
#     server {
#         listen 8080;
#         server_name localhost;

#         # Large file upload settings
#         client_max_body_size 1000M;
#         client_body_timeout 3600s;
#         client_body_buffer_size 128k;

#         # Proxy timeouts for long processing (1 hour for very large files)
#         proxy_read_timeout 3600s;
#         proxy_connect_timeout 3600s;
#         proxy_send_timeout 3600s;

#         # Disable buffering for chunked uploads
#         proxy_buffering off;
#         proxy_request_buffering off;

#         # Backend API - proxy to Flask on port 5001
#         location /api/ {
#             proxy_pass http://localhost:5001/api/;
#             proxy_http_version 1.1;

#             # Pass through headers
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;

#             # Important for chunked uploads
#             proxy_set_header Transfer-Encoding $http_transfer_encoding;
#             proxy_set_header X-Chunk-Index $http_x_chunk_index;
#             proxy_set_header X-Total-Chunks $http_x_total_chunks;
#             proxy_set_header X-Expires-At $http_x_expires_at;
#             proxy_set_header X-Signature $http_x_signature;

#             # CORS headers (in case needed)
#             add_header Access-Control-Allow-Origin * always;
#             add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
#             add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Chunk-Index, X-Total-Chunks, X-Expires-At, X-Signature" always;

#             # Handle preflight requests
#             if ($request_method = OPTIONS) {
#                 return 204;
#             }
#         }

#         # Frontend - proxy to Vite dev server on port 3000
#         location / {
#             proxy_pass http://localhost:3000/;
#             proxy_http_version 1.1;

#             # WebSocket support for Vite HMR
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "upgrade";

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }
#     }
# }


server {
    listen 80;

    server_name _;

    client_max_body_size 1000M;

    proxy_read_timeout 3600s;
    proxy_connect_timeout 3600s;
    proxy_send_timeout 3600s;

    location /api/ {
        proxy_pass http://127.0.0.1:5001/api/;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        root /var/www/rani/frontend/dist;
        index index.html;

        try_files $uri $uri/ /index.html;
    }
}
